# Sólo para correr el backend SUELTO (fuera de docker compose).
# Dentro de compose estas variables las inyecta el docker-compose.yml.
DATABASE_URL=postgres://postgres:postgres@localhost:5432/app
JWT_SECRET=cambiame-por-un-secreto-largo-y-random
ADMIN_EMAIL=admin@gamerate.local
ADMIN_PASSWORD=admin123
PORT=8080
