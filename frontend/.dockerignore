# Lo que NO entra al contexto de build del frontend.
#
# 🔴 node_modules/ acá es todavía más crítico que en el backend: Vite usa
# binarios compilados por plataforma (esbuild, rollup). Si se copian los de tu
# máquina encima de los que instaló "npm ci" para Linux, el build muere con
# "You installed esbuild for another platform" o
# "Cannot find module @rollup/rollup-linux-x64-musl".
node_modules/
dist/
.git/
